## v1.8.4

**何もしていないときの CPU 消費を約 1/4 に減らしました。** ノート PC のバッテリー持ちに効きます。

### 改善

- **アイドル時の CPU 消費を約 75% 削減しました**: タブを開いたまま何も操作していない状態でも、racker は CPU を使い続けていました（1 コアの約 2%）。原因は「出力が無くても定期的に目を覚ます処理」が、タブの数だけ動いていたことです。8 タブ開いていると、合計で毎秒 500 回以上 CPU を起こしていた計算になります。

  出力が流れている間はこれまでどおりの速さで動き、**止まっている間だけ休むように**しました。出力が再開した瞬間は別の仕組みで検知しているので、**表示が遅くなることはありません**。

  実測で、アイドル時の CPU が約 2.0% から約 0.5% になりました。

- **WSL を眠らせるようにしました**: Claude の状態を確認するため、これまで 2 秒ごとに WSL の中を覗きに行っていました。この操作は停止中の WSL を起こしてしまうため、WSL が永久に眠れない状態でした。Windows 側の確認はこれまでどおり 2 秒ごと、**WSL 側だけ 10 秒に 1 回**に減らしています。

### 変わらないもの

- ターミナルの表示速度は変わりません（出力再開の検知は別経路のため）。
- メモリ使用量は元々問題がなかったため、手を入れていません（4 分間の計測で 57MB のまま横ばい、リークなし）。

### 内部の変更（動作に影響しないもの）

- 「何も操作していない状態が続いた後でも、最初の出力が 0.6 秒以内に届くこと」を確認するテストを追加しました。省電力と引き換えに体感が悪くなる変更が入ったら、テストが落ちます。
- シェルが終了したことをタブが検知するまでの間隔を 0.1 秒から 0.5 秒に変更しました（この分だけ反映が遅れますが、体感できる差ではありません）。

### 既知の問題

- Authenticode コード署名は未対応のため、SmartScreen 警告は引き続き出ます。「詳細情報」→「実行」で続行可能。
