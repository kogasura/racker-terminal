## v1.9.4

**自動更新が効かなくなっていた問題を直しました。**

### 修正

- **「今すぐ再起動」を押しても新しいバージョンにならない問題を解消**: 更新の通知は出て、ダウンロードも終わっているのに、再起動するとバージョンが元のまま。インストーラの画面も出ず、エラーも出ないので、押したこと自体が無かったかのように見えていました。v1.9.1 以降からの自動更新がすべてこの状態でした。

  原因は racker 自身にありました。v1.9.1 で「racker を閉じたらタブのプロセスも確実に終わらせる」仕組みを入れたのですが、その道連れの対象に**更新インストーラまで含まれていました**。更新は「インストーラを起動してから racker を終了する」順で進むため、起動した直後のインストーラが道連れで消されていた、というものです。

  インストーラだけは道連れの対象から外すようにしました。タブのプロセスを確実に終わらせる動作は、これまでどおりです。

- **更新が適用されなかったときに知らせるようにしました**: これまでは失敗しても何も表示されなかったため、気付く手立てがありませんでした。次の起動時にバージョンを確かめて、変わっていなければお知らせします。

### 更新のしかた（今回だけお手数をおかけします）

**この修正は v1.9.4 に入っていますが、いまお使いのバージョン側には入っていないため、アプリ内の「今すぐ再起動」では同じ理由で失敗します。** お手数ですが、今回だけリリースページからインストーラをダウンロードして実行してください。

このとき、**インストーラは racker のタブの中からではなく、エクスプローラーなどから実行してください。** タブの中から実行すると、同じ道連れの問題を踏みます。

v1.9.4 以降は、アプリ内の更新が今までどおり使えます。

### 既知の問題

- Authenticode コード署名は未対応のため、SmartScreen 警告は引き続き出ます。「詳細情報」→「実行」で続行可能。
