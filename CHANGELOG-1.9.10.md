## v1.9.10

**長く使っていると文字が化ける問題に、根本から対処しました。**

### 修正

- **文字が空白になったり別の字に化けたりする問題**: v1.9.8 でも対処しましたが、直りきっていませんでした。今回、原因の入り口そのものを塞いでいます。

  racker は文字の形を 1 枚の画像に焼き込んで全タブで使い回しています。これまでは 10 分ごとにその画像を「消して詰め直す」掃除をしていましたが、この操作は**消したタブの表示しか整えません**。他のタブは古い位置を指したままなので、そこに別の字が焼き直された瞬間に化けます。掃除の対象を全タブにしても 1 タブにしても化けが出ていたのは、対象の数ではなく「共有している画像を横から書き換える」やり方自体に無理があったためでした。

  今回は掃除をやめ、**画像ごと作り直す**ようにしました。全タブの描画を一度畳んでから表示中のタブだけを描き直すので、どのタブも古い位置を持ち越しません。**原因が何であれ、化けた表示は 10 分以内に必ず元に戻ります。** GPU の描画が落ちて元に戻れなくなっていたタブも、同じ仕組みで復帰します。

- **タブが多いと定期的に固まる問題**: 30 秒ごとに全タブの画面を保存しており、タブ数に比例して操作が止まっていました。**前回から出力があったタブだけ**保存するようにしたので、開いているだけのタブは対象外になります。

- **配色が見づらくなることがある問題**: vim などの一部のツールは、起動時に端末へ「背景は何色か」を尋ねて明るい配色か暗い配色かを決めます。racker はこの問い合わせに答えていなかったため、明るい背景と誤解されて読みにくい配色が選ばれることがありました。答えを返すようにしています。

  あわせて、背景を透過させているときに反転表示や選択範囲の色が黒を基準に で計算されていたのを直しました。背景の見た目自体は変わりません。

- **全画面のツールで表示が崩れる問題**: GPU 描画が落ちたときに、その通知を端末の画面へ直接書き込んでいました。Claude Code のような全画面のツールを使っている最中だと表示が崩れるため、通知は内部のログだけに留めます。

### 補足

- 画像の作り直しは操作の手が空いた時間を狙って行うため、入力や出力の最中に割り込むことはありません。
- 設定の「GPU で描画する」は引き続き利用できます。今回の修正で必要になる場面はほぼ無くなるはずですが、逃げ道としてそのまま残しています。
- 背景透明度を 1.00 未満にしている間、GPU 描画が使われないのはこれまでどおりです。

### 既知の問題

- Authenticode コード署名は未対応のため、SmartScreen 警告は引き続き出ます。「詳細情報」→「実行」で続行可能。
