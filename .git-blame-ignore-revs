# git blame から除外するリビジョン（整形のみのコミット）。
#
# GitHub の blame ビューは自動でこのファイルを参照する。
# ローカルでも効かせる場合:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs

# ci: rustfmt / clippy を仕組みとして導入 (#67) — cargo fmt 一括適用を含む
f93b191b43b326f50615d428fb2412a893d3ae83
