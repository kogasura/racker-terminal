## v1.9.0

**いま何で動いているかが、画面の下に出るようになりました。**

### 新機能

- **画面下部に Claude の状況が出ます**: 表示中のタブで動いている Claude の**モデル・reasoning effort・コンテキストの消費量**と、**プランの使用量（5 時間ウィンドウ / 週次）**が 1 行で表示されます。

  ```
  Opus 5  high  ▮▮▮▯▯ 225k / 1.0M (23%)              5h 16%   週 23%
  ```

  同じことを Claude Code の中で調べるには `/status` や `/usage` を打つ必要があり、そのたびに会話が途切れていました。racker はタブの外側にいるので、会話を邪魔せずに出しておけます。

  - コンテキストと使用量は、残りが少なくなると色が変わります（75% で黄、90% で赤）
  - リセット時刻は使用量にカーソルを合わせると出ます
  - Claude を起動していないタブでは使用量だけの表示になります
  - 設定の ⚙ →「画面下部に Claude の状況を表示する」で消せます

### 改善

- **ポーリングの取りこぼしを防ぎました**: 2 秒ごとの Claude セッション確認に、前回が終わるまで次を出さないガードを入れました。WSL 側の確認は 1 回に数秒かかることがあり、これまでは要求が積み上がる可能性がありました。

### 動作について

- ステータスバーの情報は、**表示中のタブぶんだけ**を 6 秒ごとに読みます。会話ログは 50MB を超えることがあるため、ファイル末尾だけを読む作りにしています（実測 0.5ms 程度）。
- プランの使用量は 5 分ごと、およびウィンドウが前面に戻ったときに取得します。**バックグラウンドにいる間は取りに行きません**。
- 常駐メモリは増えません。読み取りに使うのは毎回すぐ解放される一時領域だけです。

### 既知の制約

- **コンテキストの上限表示は推定です**。会話ログに上限が記録されておらず、モデル名からも 1M 版かどうかを判別できません。既定を 200k とし、実際の消費がそれを超えた時点で 1M に切り替えます。1M 版を使い始めた直後は、残りが実際より少なく見えることがあります。
- モデル・effort・コンテキスト量は Claude Code の内部ファイル、使用量は非公開の API から読んでいます。**どちらも公式に約束された経路ではない**ため、Claude Code 側の変更で読めなくなる可能性があります。その場合は該当部分が消えるだけで、ターミナルの動作には影響しません。
- Claude Code にログインしていない場合、使用量は表示されません。
- Authenticode コード署名は未対応のため、SmartScreen 警告は引き続き出ます。「詳細情報」→「実行」で続行可能。
